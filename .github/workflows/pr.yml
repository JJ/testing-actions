on:
  pull_request

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@0.2.0
        with:
          github-token: ${{github.token}}
          script: |
            const creator = context.payload.sender.login
            await github.issues.createComment( {...context.issue, body: 'OK, recibido'})
